# dependencies / build output
node_modules/
dist/

# Tauri / Rust (icons ARE committed, like a standard Tauri project)
src-tauri/target/
src-tauri/gen/

# updater signing key (NEVER commit) -- keep a secure backup elsewhere
.tauri/
*.key

# misc
.DS_Store
*.log
