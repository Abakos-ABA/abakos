{
  "name": "abakos-site",
  "version": "4.0.0",
  "description": "Abakos product website, public demo, and status surfaces",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && playwright test",
    "test:headed": "npm run build && playwright test --headed"
  },
  "keywords": [
    "pos",
    "ai-compute",
    "depin"
  ],
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
